# Account baseline

Applies the whole baseline with alarms, a budget, a security contact and one
GitHub Actions deploy role.

Replace the email addresses, the phone number and `my-org/my-service` before
applying. Read the cost note in the root README first: AWS Config and GuardDuty
are the two line items that will show up on the bill.
